import React, {Component} from 'react';
import { Button, Text, View } from 'react-native';
import Maps from '../component/Maps';
export default class PublicacionViaje extends Component {
  render() {
    return (
      <Maps/>
    );
  }
}